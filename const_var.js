// var

var v = 1;

// 누군가가 v 가 변수로 선언된지 모르고 v 를 변수로 선언
v = 2;

console.log('v :', v);

// const

const c = 1;

c = 2;

// 누군가가 c 가 변수로 선언된지 모르고 c 를 변수로 선언한다면 오류가 난다.

// 변수와 상수와 다른점은 변수는 변수의 이름이 가리키는 값이 계속 바뀔 수 있지만
// 상수는 한번 어떠한것을 가리키게 되면 값을 변경이 안되며 변경하려하면 오류가 난다.